<?php
/**
 * Minimal stub for think\db\BaseQuery.
 * Required by yangweijie/think-orm-async's AsyncContext constructor type hint.
 * This project only uses raw queries, so the ORM query path is never executed.
 */
namespace think\db;

class BaseQuery
{
    // No implementation needed - only used as a type hint
}
